package com.demo.flightbooking.enums;

/**
 * An enumeration of the supported web browsers for test execution.
 * Using an enum prevents errors from typos and makes the framework configuration
 * more robust and readable.
 */
public enum BrowserType {
    CHROME,
    FIREFOX,
    EDGE;
}